#!/usr/bin/env bash
set -euo pipefail

printf '%s\n' "Pooly Sentinel uninstall stub"
printf '%s\n' "Task 1 does not install files or enable services, so there is nothing to remove."
printf '%s\n' "Future uninstall work must avoid printing secret values."
