#!/usr/bin/env bash
set -euo pipefail

printf '%s\n' "Pooly Sentinel install stub"
printf '%s\n' "Task 1 creates structure only. No binary, config, service, secrets, or receivers are installed."
printf '%s\n' "Future install work must preserve secret redaction and leave paid receivers disabled by default."
